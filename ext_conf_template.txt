# cat=basic/enable/10; type=boolean; label=Enable Cloudflare Integration:Enable or disable the Cloudflare cache purge functionality globally.
enabled = 1

# cat=basic/behavior/20; type=boolean; label=Purge on Clear All Caches:Automatically purge Cloudflare cache when "Clear all caches" is clicked in TYPO3 backend.
purgeOnClearAll = 1

# cat=basic/behavior/30; type=boolean; label=Auto-Purge on Content Change:Automatically purge affected pages when content (pages, tt_content) is saved.
autoPurgeOnSave = 1

# cat=basic/behavior/40; type=boolean; label=Enable Cache-Control Middleware:Set Cache-Control headers to prevent Cloudflare caching for backend users and protected pages.
enableMiddleware = 1

# cat=advanced/debug/10; type=boolean; label=Debug Mode:Enable verbose logging for troubleshooting. Logs will be written to var/log/typo3_ignitercf_*.log
debug = 0

# cat=advanced/logging/20; type=options[All Calls=all,Errors Only=errors_only,Disabled=none]; label=Cloudflare Log Level:What to log to the dedicated Cloudflare log file (var/log/typo3_ignitercf_cloudflare.log).
logLevel = errors_only

# cat=advanced/logging/30; type=int+; label=Log Retention Days:Number of days to keep log entries. Older entries are deleted by the cleanup scheduler task. Set to 0 to keep logs indefinitely.
logRetentionDays = 30

# cat=advanced/notification/40; type=string; label=Error Notification Emails:Comma-separated list of email addresses to notify when scheduler tasks fail. Leave empty to disable email notifications.
errorNotificationEmails =

# cat=advanced/notification/50; type=string; label=Notification Sender Email:Email address used as sender for error notifications. Leave empty to use system default.
errorNotificationSenderEmail =

# cat=advanced/notification/60; type=string; label=Notification Sender Name:Name used as sender for error notifications.
errorNotificationSenderName = IgniterCF

# ---------------------------------------------------------------------------------
# IMPORTANT: Zone ID and API Token are configured per-site, not here!
# ---------------------------------------------------------------------------------
#
# Zone ID: Add to your site's config.yaml:
#   cloudflare:
#     zoneId: "your-cloudflare-zone-id"
#
# API Token: Set via environment variable (recommended for security):
#   IGNITERCF_TOKEN_{SITE_IDENTIFIER}  (e.g. IGNITERCF_TOKEN_MAIN for site "main")
#   IGNITERCF_API_TOKEN                (global fallback for single-domain setups)
#
# For multi-domain installations, each site can have its own Cloudflare zone.
# ---------------------------------------------------------------------------------
